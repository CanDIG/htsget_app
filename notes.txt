- GETTING FILE INDEX: Algorithmn to find the last index of a file could be intensive for large files

- Does the user know before hand whether the ID is a read or variant?

- When do we delete downloaded minio file

Options for dealing wiht indexed files
-Always store indexed file in the same S3 bucket

1) the format and file extension are stored in the data_object in DRS

